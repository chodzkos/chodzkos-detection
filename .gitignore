# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Virtualenv / uv
.venv/
# uv.lock jest CELOWO śledzony (spójnie z gui-kit) — pre-commit i CI wołają
# `uv run --extra dev`, więc zablokowany toolchain = reprodukowalne dev/CI.

# Test / type cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
